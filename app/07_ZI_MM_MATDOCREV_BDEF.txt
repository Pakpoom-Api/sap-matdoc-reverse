*&---------------------------------------------------------------------*
*& Behavior Definition: ZI_MM_MATDOCREV
*& Type   : Unmanaged — Action-Only (no custom persistent table)
*& Source : I_MaterialDocumentTP (SAP Standard Released BO)
*& Pattern: Read from standard TP view, execute standard cancel action
*&---------------------------------------------------------------------*

unmanaged implementation in class ZBP_I_MM_MATDOCREV unique;
strict ( 2 );

" ── No draft: single-step action, no edit workflow needed ──
" ── No create / update / delete: read-only + action only ──

define behavior for ZI_MM_MATDOCREV alias MatDocRev
lock master unmanaged
authorization master ( global )
{
  " ── Instance-conditional action ──
  " Button disabled automatically when IsReversed = 'X' via get_instance_features
  action ( features : instance ) ReverseMaterialDocument
    result [1] $self;

  " ── Child association (read-only navigation) ──
  association _item { }
}

define behavior for ZI_MM_MATDOCREV_ITEM alias MatDocRevItem
lock dependent( MatDocRev ) by _MatDocRev
authorization dependent( MatDocRev ) by _MatDocRev
{
  " ── Read-only child — no operations exposed ──
  association _MatDocRev { }
}
